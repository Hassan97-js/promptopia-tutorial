import { PromptPost } from "@/app/create-prompt/page";

const PromptForm = ({
  formAction,
  onCreatePromptPost,
  onPromptPostChange,
  post,
  submitting
}: PromptFormProps) => {
  return <h1 className="text-4xl">PromptForm</h1>;
};

export default PromptForm;
